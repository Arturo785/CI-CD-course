package com.example.materialcalculator


// this one is only used in the debug apk when we generated because is in the debug main folder
// the other variants access the other main source folder
// the files we can access depends on the build variant we are using
object Constants {
    const val BASE_URL = "TEST_URL"
}